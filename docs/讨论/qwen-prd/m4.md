下一步进入：

```text
M4：全文搜索 + 文件名搜索
```

这一阶段目标：

```text
1. 建立全文索引内容来源：
   - 转录 txt
   - AI 总结 summary.md
   - 用户笔记 notes.md
   - 文档解析结果 parsed.md，预留
   - 普通 Markdown / TXT 文档

2. 将内容分块写入 chunks 表。

3. 支持文件名搜索。

4. 支持内容搜索。

5. 搜索结果可以跳转到资产详情页。

6. 提供手动重建全文索引按钮。

7. 转录完成后，可选自动刷新全文索引。
```

注意：这一阶段先不做向量搜索。向量搜索放到 M5，使用 LanceDB。

---

# 1. 增加文本读取工具

在 `app/utils.py` 末尾增加：

```python
def read_text_for_index(path_str: str, max_chars: int = 500_000) -> str:
    """
    读取文本文件用于建立索引。

    优先尝试 UTF-8，失败后尝试 GBK。
    """
    path = Path(path_str)

    if not path.exists():
        raise FileNotFoundError(f"文件不存在：{path}")

    encodings = ["utf-8", "utf-8-sig", "gbk"]

    for encoding in encodings:
        try:
            with open(path, "r", encoding=encoding) as f:
                return f.read(max_chars)
        except UnicodeDecodeError:
            continue

    raise ValueError(f"无法识别文本编码：{path}")
```

---

# 2. 新增全文索引服务

## app/services/index_service.py

```python
import uuid
from datetime import datetime, timezone

from app.database import get_conn
from app.state import get_db_path
from app.utils import read_text_for_index


CHUNK_MAX_CHARS = 800

INDEX_ARTIFACT_KINDS = (
    "transcript",
    "summary",
    "note",
    "parsed",
)


def utcnow_iso() -> str:
    return datetime.now(timezone.utc).isoformat()


def chunk_text(text: str, max_chars: int = CHUNK_MAX_CHARS) -> list[str]:
    """
    简单分块：

    1. 优先按段落。
    2. 段落过长时按字符切。
    """
    text = text.strip()

    if not text:
        return []

    paragraphs = [
        paragraph.strip()
        for paragraph in text.splitlines()
        if paragraph.strip()
    ]

    chunks: list[str] = []
    current = ""

    for paragraph in paragraphs:
        if len(current) + len(paragraph) + 1 <= max_chars:
            if current:
                current = current + "\n" + paragraph
            else:
                current = paragraph
            continue

        if current:
            chunks.append(current)
            current = ""

        if len(paragraph) <= max_chars:
            current = paragraph
        else:
            start = 0

            while start < len(paragraph):
                end = min(start + max_chars, len(paragraph))
                chunks.append(paragraph[start:end])
                start = end

    if current:
        chunks.append(current)

    return chunks


def insert_chunks(
    conn,
    *,
    asset_id: str,
    artifact_id: str | None,
    kind: str,
    relative_path: str,
    text: str,
) -> int:
    """
    将一段文本分块后写入 chunks 表。
    """
    chunks = chunk_text(text)

    if not chunks:
        return 0

    now = utcnow_iso()

    for index, chunk in enumerate(chunks):
        chunk_id = str(uuid.uuid4())

        conn.execute(
            """
            INSERT INTO chunks (
              id,
              asset_id,
              artifact_id,
              kind,
              relative_path,
              chunk_index,
              content,
              content_hash,
              created_at
            )
            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
            """,
            (
                chunk_id,
                asset_id,
                artifact_id,
                kind,
                relative_path,
                index,
                chunk,
                None,
                now,
            ),
        )

    return len(chunks)


def rebuild_fulltext_index() -> dict:
    """
    重建全文索引。

    当前策略：

    1. 清空 chunks。
    2. 重新索引 artifacts。
    3. 重新索引可直接读取的文档资产。
    4. 重建 FTS5。
    """
    db_path = get_db_path()
    conn = get_conn(db_path)

    stats = {
        "sources": 0,
        "chunks": 0,
    }

    try:
        conn.execute("DELETE FROM chunks")

        # 索引派生文件：
        # transcript / summary / note / parsed
        artifact_rows = conn.execute(
            """
            SELECT
              id,
              asset_id,
              kind,
              relative_path,
              absolute_path
            FROM artifacts
            WHERE status = 'active'
              AND kind IN (?, ?, ?, ?)
            """,
            INDEX_ARTIFACT_KINDS,
        ).fetchall()

        for row in artifact_rows:
            try:
                text = read_text_for_index(row["absolute_path"])
            except Exception:
                continue

            if not text.strip():
                continue

            chunk_count = insert_chunks(
                conn,
                asset_id=row["asset_id"],
                artifact_id=row["id"],
                kind=row["kind"],
                relative_path=row["relative_path"],
                text=text,
            )

            if chunk_count > 0:
                stats["sources"] += 1
                stats["chunks"] += chunk_count

        # 索引可直接读取的文档资产：
        # .md / .txt
        document_rows = conn.execute(
            """
            SELECT
              id,
              relative_path,
              absolute_path
            FROM assets
            WHERE type = 'document'
              AND parse_status = 'not_required'
              AND (
                lower(relative_path) LIKE '%.md'
                OR lower(relative_path) LIKE '%.txt'
              )
            """
        ).fetchall()

        for row in document_rows:
            try:
                text = read_text_for_index(row["absolute_path"])
            except Exception:
                continue

            if not text.strip():
                continue

            chunk_count = insert_chunks(
                conn,
                asset_id=row["id"],
                artifact_id=None,
                kind="document",
                relative_path=row["relative_path"],
                text=text,
            )

            if chunk_count > 0:
                stats["sources"] += 1
                stats["chunks"] += chunk_count

        # 重建 FTS5。
        #
        # 注意：
        # SQLite FTS5 对中文分词能力有限。
        # 当前搜索服务会同时使用 FTS 和 LIKE。
        # FTS 这里先保留，后续可以继续优化。
        try:
            conn.execute(
                """
                INSERT INTO chunks_fts(chunks_fts)
                VALUES ('rebuild')
                """
            )
        except Exception:
            pass

        conn.commit()
    finally:
        conn.close()

    return stats
```

---

# 3. 新增搜索服务

## app/services/search_service.py

```python
import sqlite3

from app.database import get_conn
from app.state import get_db_path


def escape_like(value: str) -> str:
    """
    转义 LIKE 查询中的特殊字符。
    """
    return (
        value
        .replace("\\", "\\\\")
        .replace("%", "\\%")
        .replace("_", "\\_")
    )


def build_fts_query(query: str) -> str | None:
    """
    构造简单的 FTS5 查询。

    例如：

    本地 知识 管理

    变成：

    "本地" OR "知识" OR "管理"
    """
    terms = [
        term.replace('"', "").strip()
        for term in query.split()
        if term.strip()
    ]

    if not terms:
        return None

    return " OR ".join(f'"{term}"' for term in terms)


def make_snippet(content: str, query: str, radius: int = 80) -> str:
    """
    生成搜索结果片段。
    """
    if not content:
        return ""

    query_lower = query.lower()
    content_lower = content.lower()

    index = content_lower.find(query_lower)

    if index < 0:
        snippet = content[:160].replace("\n", " ")
        return snippet

    start = max(0, index - radius)
    end = min(len(content), index + len(query) + radius)

    prefix = "..." if start > 0 else ""
    suffix = "..." if end < len(content) else ""

    snippet = content[start:end].replace("\n", " ")

    return prefix + snippet + suffix


def search_filename(conn, query: str, limit: int = 50) -> list[dict]:
    """
    文件名搜索。
    """
    like_pattern = f"%{escape_like(query)}%"

    rows = conn.execute(
        """
        SELECT
          id AS asset_id,
          title AS asset_title,
          type AS asset_type,
          relative_path
        FROM assets
        WHERE title LIKE ? ESCAPE '\\'
           OR relative_path LIKE ? ESCAPE '\\'
        ORDER BY title
        LIMIT ?
        """,
        (
            like_pattern,
            like_pattern,
            limit,
        ),
    ).fetchall()

    results = []

    for row in rows:
        results.append(
            {
                "asset_id": row["asset_id"],
                "asset_title": row["asset_title"],
                "asset_type": row["asset_type"],
                "kind": "asset",
                "relative_path": row["relative_path"],
                "snippet": row["relative_path"],
            }
        )

    return results


def search_fulltext(conn, query: str, limit: int = 50) -> list[dict]:
    """
    全文内容搜索。

    当前策略：

    1. 先尝试 SQLite FTS5。
    2. 再使用 LIKE 兜底。
       这样中文子串搜索更可靠。
    """
    results: list[dict] = []
    seen_chunk_ids: set[str] = set()

    fts_query = build_fts_query(query)

    if fts_query:
        try:
            rows = conn.execute(
                """
                SELECT
                  c.id AS chunk_id,
                  c.asset_id AS asset_id,
                  c.kind AS kind,
                  c.relative_path AS relative_path,
                  c.content AS content,
                  a.title AS asset_title,
                  a.type AS asset_type
                FROM chunks_fts f
                JOIN chunks c ON c.rowid = f.rowid
                LEFT JOIN assets a ON a.id = c.asset_id
                WHERE chunks_fts MATCH ?
                ORDER BY rank
                LIMIT ?
                """,
                (
                    fts_query,
                    limit,
                ),
            ).fetchall()

            for row in rows:
                if len(results) >= limit:
                    break

                chunk_id = row["chunk_id"]

                if chunk_id in seen_chunk_ids:
                    continue

                seen_chunk_ids.add(chunk_id)

                results.append(
                    {
                        "asset_id": row["asset_id"],
                        "asset_title": row["asset_title"] or row["relative_path"],
                        "asset_type": row["asset_type"],
                        "kind": row["kind"],
                        "relative_path": row["relative_path"],
                        "snippet": make_snippet(row["content"], query),
                    }
                )
        except sqlite3.OperationalError:
            pass

    like_pattern = f"%{escape_like(query)}%"

    rows = conn.execute(
        """
        SELECT
          c.id AS chunk_id,
          c.asset_id AS asset_id,
          c.kind AS kind,
          c.relative_path AS relative_path,
          c.content AS content,
          a.title AS asset_title,
          a.type AS asset_type
        FROM chunks c
        LEFT JOIN assets a ON a.id = c.asset_id
        WHERE c.content LIKE ? ESCAPE '\\'
        ORDER BY c.id
        LIMIT ?
        """,
        (
            like_pattern,
            limit,
        ),
    ).fetchall()

    for row in rows:
        if len(results) >= limit:
            break

        chunk_id = row["chunk_id"]

        if chunk_id in seen_chunk_ids:
            continue

        seen_chunk_ids.add(chunk_id)

        results.append(
            {
                "asset_id": row["asset_id"],
                "asset_title": row["asset_title"] or row["relative_path"],
                "asset_type": row["asset_type"],
                "kind": row["kind"],
                "relative_path": row["relative_path"],
                "snippet": make_snippet(row["content"], query),
            }
        )

    return results


def search(query: str, mode: str, limit: int = 50) -> list[dict]:
    """
    统一搜索入口。

    mode:
      filename
      fulltext
    """
    query = query.strip()

    if not query:
        return []

    db_path = get_db_path()
    conn = get_conn(db_path)

    try:
        if mode == "filename":
            return search_filename(conn, query, limit=limit)

        return search_fulltext(conn, query, limit=limit)
    finally:
        conn.close()
```

---

# 4. 新增搜索页

## app/ui/pages/search.py

```python
from nicegui import run, ui

from app.services import index_service, search_service
from app.state import state


KIND_LABELS = {
    "asset": "文件名",
    "document": "文档",
    "transcript": "转录",
    "summary": "总结",
    "note": "笔记",
    "parsed": "解析结果",
    "transcript_meta": "转录元数据",
    "meta": "元数据",
}


@ui.page("/search")
def search_page():
    if state.library_root is None:
        ui.label("未打开知识库").classes("text-xl")
        ui.link("去打开知识库", "/").classes("text-blue-600")
        return

    ui.label("搜索").classes("text-2xl font-bold")
    ui.label(str(state.library_root)).classes("text-sm text-gray-500")

    with ui.row().classes("items-end gap-3 mt-3"):
        query_input = ui.input("搜索关键词").classes("w-96")

        mode_select = ui.select(
            ["全文", "文件名"],
            value="全文",
            label="搜索模式",
        )

        search_button = ui.button("搜索")
        rebuild_button = ui.button("重建全文索引")

        ui.link("资产列表", "/assets").classes("text-blue-600")
        ui.link("任务中心", "/tasks").classes("text-blue-600")
        ui.link("首页", "/").classes("text-blue-600")

    ui.label(
        "首次使用全文搜索前，建议先点击“重建全文索引”。"
    ).classes("text-sm text-gray-600 mt-2")

    results_container = ui.column().classes("w-full mt-4")

    def render_results(results: list[dict]):
        results_container.clear()

        with results_container:
            if not results:
                ui.label("没有搜索结果。").classes("text-gray-600")
                return

            for result in results:
                with ui.card().classes("w-full p-3"):
                    with ui.row().classes("w-full items-center gap-2"):
                        if result["asset_id"]:
                            ui.link(
                                result["asset_title"],
                                f"/assets/{result['asset_id']}",
                            ).classes(
                                "text-blue-600 font-semibold"
                            )
                        else:
                            ui.label(result["asset_title"]).classes(
                                "font-semibold"
                            )

                        ui.badge(
                            KIND_LABELS.get(result["kind"], result["kind"]),
                            color="grey",
                        )

                        if result.get("asset_type"):
                            ui.badge(result["asset_type"], color="gray")

                    ui.label(result["relative_path"]).classes(
                        "text-xs text-gray-500 mt-1 truncate"
                    )

                    ui.label(result["snippet"]).classes(
                        "text-sm mt-2 whitespace-pre-wrap"
                    )

    async def handle_search():
        query = query_input.value

        if not query or not query.strip():
            ui.notify("请输入搜索关键词", type="warning")
            return

        mode = "filename" if mode_select.value == "文件名" else "fulltext"

        search_button.disable()

        try:
            results = await run.io_bound(
                search_service.search,
                query,
                mode,
            )

            render_results(results)
        except Exception as exc:
            ui.notify(str(exc), type="negative")
        finally:
            search_button.enable()

    async def handle_rebuild():
        rebuild_button.disable()

        try:
            stats = await run.io_bound(index_service.rebuild_fulltext_index)

            ui.notify(
                "全文索引重建完成："
                f"{stats['sources']} 个来源，"
                f"{stats['chunks']} 个片段",
                type="positive",
            )
        except Exception as exc:
            ui.notify(str(exc), type="negative")
        finally:
            rebuild_button.enable()

    search_button.on_click(handle_search)
    rebuild_button.on_click(handle_rebuild)

    query_input.on("keydown.enter", handle_search)
```

---

# 5. 修改 main.py

加入搜索页。

```python
from nicegui import app, ui

from app.api.library import register_library_api
from app.ui.pages import (
    asset_detail_page,
    assets_page,
    home_page,
    search_page,
    tasks_page,
)  # noqa: F401


register_library_api(app)


if __name__ in {"__main__", "__mp_main__"}:
    ui.run(
        title="Local Knowledge Hub",
        host="127.0.0.1",
        port=8765,
        show=False,
        reload=False,
    )
```

---

# 6. 可选：转录成功后自动重建全文索引

如果你希望转录完成后马上能搜索到内容，可以修改：

```text
app/services/transcription_service.py
```

在文件顶部加入：

```python
from app.services.index_service import rebuild_fulltext_index
```

然后找到这一段：

```python
try:
    scan_current_library()
except Exception as scan_exc:
    warning = f"转录成功，但刷新索引失败：{scan_exc}"
```

改成：

```python
try:
    scan_current_library()
    rebuild_fulltext_index()
except Exception as scan_exc:
    warning = f"转录成功，但刷新索引失败：{scan_exc}"
```

这样每次转录成功后会自动：

```text
1. 重新扫描文件
2. 识别新的 transcript artifact
3. 重建全文索引
```

如果你的知识库很大，重建索引会比较慢。那时候可以改成只索引当前资产。当前个人使用阶段，全量重建更简单稳定。

---

# 7. 给资产列表页加搜索入口，可选

你可以在 `app/ui/pages/assets.py` 里找到：

```python
ui.link("首页", "/").classes("text-blue-600")
```

在它旁边加：

```python
ui.link("搜索", "/search").classes("text-blue-600")
ui.link("任务", "/tasks").classes("text-blue-600")
```

例如：

```python
with ui.row().classes("items-center gap-3 mt-3"):
    scan_button = ui.button("扫描 / 刷新")
    count_label = ui.label("").classes("text-sm text-gray-600")
    ui.link("首页", "/").classes("text-blue-600")
    ui.link("搜索", "/search").classes("text-blue-600")
    ui.link("任务", "/tasks").classes("text-blue-600")
```

---

# 8. 运行测试

重启：

```powershell
python -m app.main
```

打开：

```text
http://127.0.0.1:8765/search
```

测试步骤：

```text
1. 打开知识库。
2. 先扫描文件。
3. 确保至少有一个 transcript、summary、notes 或 md/txt 文件。
4. 点击“重建全文索引”。
5. 搜索一个出现在转录或笔记中的关键词。
6. 选择“全文”模式，确认能搜到内容。
7. 选择“文件名”模式，确认能搜到文件名。
8. 点击搜索结果标题，进入资产详情页。
```

---

# 9. M4 验收标准

你应该看到：

```text
1. 重建全文索引后，搜索页显示来源数和片段数。
2. 搜索转录文本中的关键词，可以命中 transcript。
3. 搜索总结中的关键词，可以命中 summary。
4. 搜索笔记中的关键词，可以命中 note。
5. 搜索普通 Markdown/TXT 文档内容，可以命中 document。
6. 文件名搜索可以命中标题和路径。
7. 搜索结果可以点击跳转到资产详情页。
```

---

# 10. 当前搜索策略说明

这一版全文搜索同时使用：

```text
SQLite FTS5
SQLite LIKE
```

原因是：

```text
SQLite FTS5 默认分词对中文不够友好。
LIKE 虽然慢一些，但对中文子串搜索更可靠。
```

当前个人知识库规模下，LIKE 是可接受的。

后续可以继续优化：

```text
1. 中文分词器
2. 拼音搜索
3. FTS5 trigram
4. 向量搜索兜底
```

---

# 11. 下一步 M5

M4 正常后，下一步进入：

```text
M5：LanceDB 向量搜索
```

M5 会做：

```text
1. 配置 OpenAI 兼容 Embedding API。
2. 对 chunks 生成向量。
3. 写入 LanceDB。
4. 搜索页增加“语义搜索”模式。
5. embedding 缓存，避免重复调用 API。
6. 向量索引重建。
```