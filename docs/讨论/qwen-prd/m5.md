如果 M4 已经正常，下一步进入：

```text
M5：LanceDB 向量搜索
```

这一阶段目标：

```text
1. 配置 OpenAI 兼容 Embedding API。
2. 将 M4 建立的 chunks 转成向量。
3. 写入 LanceDB。
4. 搜索页增加“语义搜索”模式。
5. 支持重建向量索引。
6. 使用 embedding 缓存，减少重复 API 调用。
```

注意：

```text
向量搜索依赖 Embedding API。
重建向量索引会调用 API，可能产生费用或额度消耗。
```

---

# 1. 安装依赖

如果你的主应用使用 uv：

```powershell
uv add lancedb httpx
```

如果使用 pip：

```powershell
pip install lancedb httpx
```

---

# 2. 配置 Embedding

打开：

```text
.knowledge/config.toml
```

在已有配置后面加入 `[embedding]`。

---

## 示例 A：使用 SiliconFlow Embedding

如果你打算复用 SiliconFlow，可以这样：

```toml
[embedding]
enabled = true
provider = "openai_compatible"
base_url = "https://api.siliconflow.cn/v1"
api_key_env = "SILICONFLOW_API_KEY"
model = "BAAI/bge-m3"
dimension = 1024
batch_size = 16
timeout = 120
```

然后在启动主应用的 PowerShell 里设置：

```powershell
$env:SILICONFLOW_API_KEY="sk-你的key"
python -m app.main
```

---

## 示例 B：使用 OpenAI Embedding

```toml
[embedding]
enabled = true
provider = "openai_compatible"
base_url = "https://api.openai.com/v1"
api_key_env = "OPENAI_API_KEY"
model = "text-embedding-3-small"
dimension = 1536
batch_size = 16
timeout = 120
```

启动前：

```powershell
$env:OPENAI_API_KEY="sk-你的key"
python -m app.main
```

---

## 重要说明

```text
dimension 必须和模型实际输出维度一致。
```

常见值：

```text
BAAI/bge-m3 -> 1024
text-embedding-3-small -> 1536
text-embedding-3-large -> 3072
```

如果维度配置错误，LanceDB 写入或搜索会失败。

---

# 3. 替换配置服务

## app/services/config_service.py

```python
import os
import tomllib
from pathlib import Path

from app.state import state


def get_config_path() -> Path:
    if state.library_root is None:
        raise RuntimeError("未打开知识库")

    return state.library_root / ".knowledge" / "config.toml"


def load_config() -> dict:
    """
    读取 .knowledge/config.toml。
    """
    path = get_config_path()

    if not path.exists():
        return {}

    with open(path, "rb") as f:
        return tomllib.load(f)


def get_transcribe_cli_config() -> dict:
    """
    读取转录 CLI 配置。
    """
    config = load_config()
    cli_config = config.get("cli", {})

    command = cli_config.get("transcribe_command")

    if not command or not isinstance(command, list):
        raise ValueError(
            "未配置 transcribe_command。"
            "请在 .knowledge/config.toml 的 [cli] 中配置。"
        )

    cwd = cli_config.get("transcribe_cwd")
    timeout = int(cli_config.get("transcribe_timeout_seconds", 14400))

    return {
        "command": command,
        "cwd": cwd,
        "timeout": timeout,
    }


def get_embedding_config() -> dict:
    """
    读取 Embedding 配置。
    """
    config = load_config()
    embedding_config = config.get("embedding", {})

    enabled = bool(embedding_config.get("enabled", False))

    base_url = str(embedding_config.get("base_url", "")).strip()
    api_key_env = str(embedding_config.get("api_key_env", "")).strip()
    api_key = str(embedding_config.get("api_key", "")).strip()

    if api_key_env:
        api_key = os.environ.get(api_key_env, api_key)

    model = str(embedding_config.get("model", "")).strip()
    dimension = int(embedding_config.get("dimension", 0) or 0)
    batch_size = int(embedding_config.get("batch_size", 16) or 16)
    timeout = int(embedding_config.get("timeout", 120) or 120)

    result = {
        "enabled": enabled,
        "provider": str(embedding_config.get("provider", "openai_compatible")),
        "base_url": base_url,
        "api_key_env": api_key_env,
        "api_key": api_key,
        "model": model,
        "dimension": dimension,
        "batch_size": batch_size,
        "timeout": timeout,
    }

    if enabled:
        if not base_url:
            raise ValueError("Embedding 配置缺少 base_url")

        if not model:
            raise ValueError("Embedding 配置缺少 model")

        if dimension <= 0:
            raise ValueError("Embedding 配置缺少有效的 dimension")

        if not api_key:
            if api_key_env:
                raise ValueError(
                    f"未获取到 Embedding API Key。"
                    f"请设置环境变量：{api_key_env}"
                )
            else:
                raise ValueError(
                    "未获取到 Embedding API Key。"
                    "请配置 api_key_env 或 api_key。"
                )

    return result
```

---

# 4. 新增向量服务

## app/services/vector_service.py

```python
import hashlib
import json
from datetime import datetime, timezone

import httpx

from app.database import get_conn
from app.services.config_service import get_embedding_config
from app.state import get_db_path, state


TABLE_NAME = "chunks"


def utcnow_iso() -> str:
    return datetime.now(timezone.utc).isoformat()


def sha256_text(text: str) -> str:
    return hashlib.sha256(text.encode("utf-8")).hexdigest()


def cache_key(model: str, text: str, prefix: str = "chunk") -> str:
    """
    embedding 缓存 key。

    格式：

    chunk:model:sha256
    query:model:sha256
    """
    return f"{prefix}:{model}:{sha256_text(text)}"


def vector_dir():
    if state.library_root is None:
        raise RuntimeError("未打开知识库")

    return state.library_root / ".knowledge" / "vector" / "lancedb"


def connect_lancedb():
    import lancedb

    path = vector_dir()
    path.mkdir(parents=True, exist_ok=True)

    return lancedb.connect(str(path))


def table_exists(db) -> bool:
    return TABLE_NAME in db.table_names()


def get_cached_vector(conn, key: str):
    row = conn.execute(
        """
        SELECT vector
        FROM embedding_cache
        WHERE content_hash = ?
        """,
        (key,),
    ).fetchone()

    if row is None:
        return None

    raw = row["vector"]

    if isinstance(raw, bytes):
        raw = raw.decode("utf-8")

    try:
        return json.loads(raw)
    except Exception:
        return None


def set_cached_vector(conn, key: str, model: str, vector: list[float]) -> None:
    conn.execute(
        """
        INSERT OR REPLACE INTO embedding_cache (
          content_hash,
          model,
          vector,
          dimension,
          created_at
        )
        VALUES (?, ?, ?, ?, ?)
        """,
        (
            key,
            model,
            json.dumps(vector).encode("utf-8"),
            len(vector),
            utcnow_iso(),
        ),
    )


def embed_texts(texts: list[str], config: dict) -> list[list[float]]:
    """
    调用 OpenAI 兼容 embedding API。
    """
    if not texts:
        return []

    url = config["base_url"].rstrip("/") + "/embeddings"

    headers = {
        "Authorization": f"Bearer {config['api_key']}",
        "Content-Type": "application/json",
    }

    all_vectors: list[list[float]] = []
    batch_size = max(1, int(config.get("batch_size", 16)))

    with httpx.Client(timeout=config.get("timeout", 120)) as client:
        for start in range(0, len(texts), batch_size):
            batch = texts[start : start + batch_size]

            payload = {
                "model": config["model"],
                "input": batch,
            }

            response = client.post(url, headers=headers, json=payload)

            if response.status_code != 200:
                raise RuntimeError(
                    f"Embedding API 错误：{response.status_code} {response.text[:500]}"
                )

            data = response.json().get("data", [])

            data.sort(key=lambda item: item.get("index", 0))

            vectors = [item["embedding"] for item in data]

            if len(vectors) != len(batch):
                raise RuntimeError(
                    "Embedding API 返回数量与请求数量不一致"
                )

            all_vectors.extend(vectors)

    return all_vectors


def embed_query(query: str, config: dict) -> list[float]:
    """
    对搜索语句生成 embedding，并尽量使用缓存。
    """
    db_path = get_db_path()
    conn = get_conn(db_path)

    key = cache_key(config["model"], query, prefix="query")

    try:
        cached = get_cached_vector(conn, key)

        if cached is not None and len(cached) == config["dimension"]:
            return cached

        vector = embed_texts([query], config)[0]

        if len(vector) != config["dimension"]:
            raise ValueError(
                f"Embedding 维度不匹配：配置为 {config['dimension']}，"
                f"实际返回 {len(vector)}"
            )

        set_cached_vector(conn, key, config["model"], vector)
        conn.commit()

        return vector
    finally:
        conn.close()


def rebuild_vector_index() -> dict:
    """
    重建 LanceDB 向量索引。

    数据来源是 SQLite chunks 表。
    """
    config = get_embedding_config()

    if not config["enabled"]:
        raise ValueError("向量搜索未启用，请检查 [embedding] enabled")

    db_path = get_db_path()
    conn = get_conn(db_path)

    records: list[dict | None] = []
    pending: list[dict] = []

    stats = {
        "total_chunks": 0,
        "cache_hits": 0,
        "embedded": 0,
    }

    try:
        rows = conn.execute(
            """
            SELECT
              id,
              asset_id,
              artifact_id,
              kind,
              relative_path,
              chunk_index,
              content
            FROM chunks
            ORDER BY rowid
            """
        ).fetchall()

        now = utcnow_iso()

        for row in rows:
            content = row["content"] or ""

            if not content.strip():
                continue

            content_hash = sha256_text(content)
            key = cache_key(config["model"], content, prefix="chunk")

            record = {
                "id": row["id"],
                "asset_id": row["asset_id"],
                "artifact_id": row["artifact_id"] or "",
                "kind": row["kind"],
                "relative_path": row["relative_path"],
                "chunk_index": row["chunk_index"],
                "content": content,
                "content_hash": content_hash,
                "embedding_model": config["model"],
                "updated_at": now,
            }

            cached_vector = get_cached_vector(conn, key)

            if cached_vector is not None and len(cached_vector) == config["dimension"]:
                record["vector"] = cached_vector
                records.append(record)
                stats["cache_hits"] += 1
                continue

            placeholder_index = len(records)
            records.append(None)

            pending.append(
                {
                    "index": placeholder_index,
                    "content": content,
                    "key": key,
                    "record": record,
                }
            )

        # 批量调用 embedding API。
        for start in range(0, len(pending), max(1, config["batch_size"])):
            batch = pending[start : start + max(1, config["batch_size"])]

            texts = [item["content"] for item in batch]
            vectors = embed_texts(texts, config)

            if len(vectors) != len(batch):
                raise RuntimeError("Embedding API 返回数量异常")

            for item, vector in zip(batch, vectors):
                if len(vector) != config["dimension"]:
                    raise ValueError(
                        f"Embedding 维度不匹配：配置为 {config['dimension']}，"
                        f"实际返回 {len(vector)}"
                    )

                record = item["record"]
                record["vector"] = vector

                records[item["index"]] = record

                set_cached_vector(conn, item["key"], config["model"], vector)

                stats["embedded"] += 1

        conn.commit()
    finally:
        conn.close()

    final_records = [record for record in records if record is not None]

    stats["total_chunks"] = len(final_records)

    db = connect_lancedb()

    if table_exists(db):
        db.drop_table(TABLE_NAME)

    if final_records:
        db.create_table(TABLE_NAME, data=final_records)

    return stats


def search_vectors(query: str, limit: int = 20) -> list[dict]:
    """
    向量搜索。
    """
    query = query.strip()

    if not query:
        return []

    config = get_embedding_config()

    if not config["enabled"]:
        raise ValueError("向量搜索未启用，请检查 [embedding] enabled")

    db = connect_lancedb()

    if not table_exists(db):
        return []

    table = db.open_table(TABLE_NAME)

    query_vector = embed_query(query, config)

    rows = table.search(query_vector).limit(limit).to_list()

    hits: list[dict] = []

    for row in rows:
        hits.append(
            {
                "asset_id": row.get("asset_id"),
                "artifact_id": row.get("artifact_id"),
                "kind": row.get("kind"),
                "relative_path": row.get("relative_path"),
                "content": row.get("content"),
                "distance": row.get("_distance"),
            }
        )

    return hits
```

---

# 5. 替换搜索服务

这一版加入向量搜索。

## app/services/search_service.py

```python
import sqlite3

from app.database import get_conn
from app.state import get_db_path


def escape_like(value: str) -> str:
    """
    转义 LIKE 查询中的特殊字符。
    """
    return (
        value
        .replace("\\", "\\\\")
        .replace("%", "\\%")
        .replace("_", "\\_")
    )


def build_fts_query(query: str) -> str | None:
    """
    构造简单的 FTS5 查询。
    """
    terms = [
        term.replace('"', "").strip()
        for term in query.split()
        if term.strip()
    ]

    if not terms:
        return None

    return " OR ".join(f'"{term}"' for term in terms)


def make_snippet(content: str, query: str, radius: int = 80) -> str:
    """
    生成搜索结果片段。
    """
    if not content:
        return ""

    query_lower = query.lower()
    content_lower = content.lower()

    index = content_lower.find(query_lower)

    if index < 0:
        snippet = content[:160].replace("\n", " ")
        return snippet

    start = max(0, index - radius)
    end = min(len(content), index + len(query) + radius)

    prefix = "..." if start > 0 else ""
    suffix = "..." if end < len(content) else ""

    snippet = content[start:end].replace("\n", " ")

    return prefix + snippet + suffix


def search_filename(conn, query: str, limit: int = 50) -> list[dict]:
    """
    文件名搜索。
    """
    like_pattern = f"%{escape_like(query)}%"

    rows = conn.execute(
        """
        SELECT
          id AS asset_id,
          title AS asset_title,
          type AS asset_type,
          relative_path
        FROM assets
        WHERE title LIKE ? ESCAPE '\\'
           OR relative_path LIKE ? ESCAPE '\\'
        ORDER BY title
        LIMIT ?
        """,
        (
            like_pattern,
            like_pattern,
            limit,
        ),
    ).fetchall()

    results = []

    for row in rows:
        results.append(
            {
                "asset_id": row["asset_id"],
                "asset_title": row["asset_title"],
                "asset_type": row["asset_type"],
                "kind": "asset",
                "relative_path": row["relative_path"],
                "snippet": row["relative_path"],
            }
        )

    return results


def search_fulltext(conn, query: str, limit: int = 50) -> list[dict]:
    """
    全文内容搜索。
    """
    results: list[dict] = []
    seen_chunk_ids: set[str] = set()

    fts_query = build_fts_query(query)

    if fts_query:
        try:
            rows = conn.execute(
                """
                SELECT
                  c.id AS chunk_id,
                  c.asset_id AS asset_id,
                  c.kind AS kind,
                  c.relative_path AS relative_path,
                  c.content AS content,
                  a.title AS asset_title,
                  a.type AS asset_type
                FROM chunks_fts f
                JOIN chunks c ON c.rowid = f.rowid
                LEFT JOIN assets a ON a.id = c.asset_id
                WHERE chunks_fts MATCH ?
                ORDER BY rank
                LIMIT ?
                """,
                (
                    fts_query,
                    limit,
                ),
            ).fetchall()

            for row in rows:
                if len(results) >= limit:
                    break

                chunk_id = row["chunk_id"]

                if chunk_id in seen_chunk_ids:
                    continue

                seen_chunk_ids.add(chunk_id)

                results.append(
                    {
                        "asset_id": row["asset_id"],
                        "asset_title": row["asset_title"] or row["relative_path"],
                        "asset_type": row["asset_type"],
                        "kind": row["kind"],
                        "relative_path": row["relative_path"],
                        "snippet": make_snippet(row["content"], query),
                    }
                )
        except sqlite3.OperationalError:
            pass

    like_pattern = f"%{escape_like(query)}%"

    rows = conn.execute(
        """
        SELECT
          c.id AS chunk_id,
          c.asset_id AS asset_id,
          c.kind AS kind,
          c.relative_path AS relative_path,
          c.content AS content,
          a.title AS asset_title,
          a.type AS asset_type
        FROM chunks c
        LEFT JOIN assets a ON a.id = c.asset_id
        WHERE c.content LIKE ? ESCAPE '\\'
        ORDER BY c.id
        LIMIT ?
        """,
        (
            like_pattern,
            limit,
        ),
    ).fetchall()

    for row in rows:
        if len(results) >= limit:
            break

        chunk_id = row["chunk_id"]

        if chunk_id in seen_chunk_ids:
            continue

        seen_chunk_ids.add(chunk_id)

        results.append(
            {
                "asset_id": row["asset_id"],
                "asset_title": row["asset_title"] or row["relative_path"],
                "asset_type": row["asset_type"],
                "kind": row["kind"],
                "relative_path": row["relative_path"],
                "snippet": make_snippet(row["content"], query),
            }
        )

    return results


def search_vector(conn, query: str, limit: int = 20) -> list[dict]:
    """
    向量语义搜索。
    """
    from app.services import vector_service

    hits = vector_service.search_vectors(query, limit=limit)

    results = []

    for hit in hits:
        asset_title = hit["relative_path"]
        asset_type = None

        if hit["asset_id"]:
            asset_row = conn.execute(
                """
                SELECT title, type
                FROM assets
                WHERE id = ?
                """,
                (hit["asset_id"],),
            ).fetchone()

            if asset_row is not None:
                asset_title = asset_row["title"]
                asset_type = asset_row["type"]

        results.append(
            {
                "asset_id": hit["asset_id"],
                "asset_title": asset_title,
                "asset_type": asset_type,
                "kind": hit.get("kind") or "vector",
                "relative_path": hit.get("relative_path"),
                "snippet": make_snippet(hit.get("content") or "", query),
                "distance": hit.get("distance"),
            }
        )

    return results


def search(query: str, mode: str, limit: int = 50) -> list[dict]:
    """
    统一搜索入口。

    mode:
      filename
      fulltext
      vector
    """
    query = query.strip()

    if not query:
        return []

    db_path = get_db_path()
    conn = get_conn(db_path)

    try:
        if mode == "filename":
            return search_filename(conn, query, limit=limit)

        if mode == "vector":
            return search_vector(conn, query, limit=limit)

        return search_fulltext(conn, query, limit=limit)
    finally:
        conn.close()
```

---

# 6. 替换搜索页

这一版加入：

```text
语义搜索模式
重建向量索引按钮
```

## app/ui/pages/search.py

```python
from nicegui import run, ui

from app.services import index_service, search_service, vector_service
from app.state import state


KIND_LABELS = {
    "asset": "文件名",
    "document": "文档",
    "transcript": "转录",
    "summary": "总结",
    "note": "笔记",
    "parsed": "解析结果",
    "transcript_meta": "转录元数据",
    "meta": "元数据",
    "vector": "向量",
}


@ui.page("/search")
def search_page():
    if state.library_root is None:
        ui.label("未打开知识库").classes("text-xl")
        ui.link("去打开知识库", "/").classes("text-blue-600")
        return

    ui.label("搜索").classes("text-2xl font-bold")
    ui.label(str(state.library_root)).classes("text-sm text-gray-500")

    with ui.row().classes("items-end gap-3 mt-3"):
        query_input = ui.input("搜索关键词").classes("w-96")

        mode_select = ui.select(
            ["全文", "文件名", "语义"],
            value="全文",
            label="搜索模式",
        )

        search_button = ui.button("搜索")
        rebuild_fulltext_button = ui.button("重建全文索引")
        rebuild_vector_button = ui.button("重建向量索引")

        ui.link("资产列表", "/assets").classes("text-blue-600")
        ui.link("任务中心", "/tasks").classes("text-blue-600")
        ui.link("首页", "/").classes("text-blue-600")

    ui.label(
        "首次使用全文搜索前，先重建全文索引；"
        "首次使用语义搜索前，先重建向量索引。"
    ).classes("text-sm text-gray-600 mt-2")

    ui.label(
        "重建向量索引会调用 Embedding API，可能产生费用或额度消耗。"
    ).classes("text-sm text-orange-600 mt-1")

    results_container = ui.column().classes("w-full mt-4")

    def render_results(results: list[dict]):
        results_container.clear()

        with results_container:
            if not results:
                ui.label("没有搜索结果。").classes("text-gray-600")
                return

            for result in results:
                with ui.card().classes("w-full p-3"):
                    with ui.row().classes("w-full items-center gap-2"):
                        if result["asset_id"]:
                            ui.link(
                                result["asset_title"],
                                f"/assets/{result['asset_id']}",
                            ).classes(
                                "text-blue-600 font-semibold"
                            )
                        else:
                            ui.label(result["asset_title"]).classes(
                                "font-semibold"
                            )

                        ui.badge(
                            KIND_LABELS.get(result["kind"], result["kind"]),
                            color="grey",
                        )

                        if result.get("asset_type"):
                            ui.badge(result["asset_type"], color="gray")

                        if result.get("distance") is not None:
                            ui.label(
                                f"distance: {result['distance']:.4f}"
                            ).classes("text-xs text-gray-500")

                    ui.label(result["relative_path"] or "").classes(
                        "text-xs text-gray-500 mt-1 truncate"
                    )

                    ui.label(result["snippet"]).classes(
                        "text-sm mt-2 whitespace-pre-wrap"
                    )

    async def handle_search():
        query = query_input.value

        if not query or not query.strip():
            ui.notify("请输入搜索关键词", type="warning")
            return

        if mode_select.value == "文件名":
            mode = "filename"
        elif mode_select.value == "语义":
            mode = "vector"
        else:
            mode = "fulltext"

        search_button.disable()

        try:
            results = await run.io_bound(
                search_service.search,
                query,
                mode,
            )

            render_results(results)
        except Exception as exc:
            ui.notify(str(exc), type="negative")
        finally:
            search_button.enable()

    async def handle_rebuild_fulltext():
        rebuild_fulltext_button.disable()

        try:
            stats = await run.io_bound(index_service.rebuild_fulltext_index)

            ui.notify(
                "全文索引重建完成："
                f"{stats['sources']} 个来源，"
                f"{stats['chunks']} 个片段",
                type="positive",
            )
        except Exception as exc:
            ui.notify(str(exc), type="negative")
        finally:
            rebuild_fulltext_button.enable()

    async def handle_rebuild_vector():
        rebuild_vector_button.disable()

        try:
            stats = await run.io_bound(vector_service.rebuild_vector_index)

            ui.notify(
                "向量索引重建完成："
                f"总片段 {stats['total_chunks']}，"
                f"缓存命中 {stats['cache_hits']}，"
                f"新调用 {stats['embedded']}",
                type="positive",
            )
        except Exception as exc:
            ui.notify(str(exc), type="negative")
        finally:
            rebuild_vector_button.enable()

    search_button.on_click(handle_search)
    rebuild_fulltext_button.on_click(handle_rebuild_fulltext)
    rebuild_vector_button.on_click(handle_rebuild_vector)

    query_input.on("keydown.enter", handle_search)
```

---

# 7. 使用顺序

第一次使用搜索时建议：

```text
1. 打开知识库。
2. 扫描文件。
3. 确保转录、总结、笔记或文本文档存在。
4. 重建全文索引。
5. 配置 Embedding API。
6. 重建向量索引。
7. 使用全文 / 文件名 / 语义搜索。
```

---

# 8. M5 验收标准

你应该看到：

```text
1. 重建全文索引成功。
2. 重建向量索引成功，并显示总片段数、缓存命中数、新调用数。
3. 再次重建向量索引时，缓存命中数增加，新调用数减少。
4. 搜索模式选择“语义”后，可以返回相似内容。
5. 搜索结果可以点击跳转到资产详情页。
6. 没有配置 Embedding API 时，会提示配置错误，而不是静默失败。
```

---

# 9. 常见问题

## 1. 提示未启用向量搜索

检查：

```toml
[embedding]
enabled = true
```

---

## 2. 提示未获取到 API Key

如果配置是：

```toml
api_key_env = "SILICONFLOW_API_KEY"
```

那么启动主应用的终端里必须有：

```powershell
$env:SILICONFLOW_API_KEY="sk-xxx"
```

然后再：

```powershell
python -m app.main
```

---

## 3. Embedding 维度不匹配

例如配置：

```toml
dimension = 1024
```

但模型实际返回 1536 维。

解决：

```text
修改 dimension 为模型真实维度。
重新构建向量索引。
```

---

## 4. 语义搜索没有结果

检查：

```text
1. 是否已经重建全文索引。
2. 是否已经重建向量索引。
3. chunks 表是否有内容。
4. LanceDB 目录是否生成：
   .knowledge/vector/lancedb
```

---

# 10. 当前不建议自动重建向量索引

转录成功后可以自动重建全文索引，但不建议自动重建向量索引。

原因：

```text
1. Embedding API 有成本。
2. 长转录会触发大量 embedding。
3. 个人库可以接受手动重建。
```

当前建议：

```text
转录 / 总结 / 笔记变化后：
  手动点击“重建全文索引”
  手动点击“重建向量索引”
```

后续可以优化为：

```text
只增量索引当前资产
按 artifact hash 判断是否变化
只对新增 chunk 调 embedding
```

---

# 11. 下一步 M6

M5 正常后，下一步进入：

```text
M6：AI 总结生成
```

M6 会做：

```text
1. 配置 OpenAI 兼容 LLM API。
2. 资产详情页增加“生成总结”按钮。
3. 音视频基于转录文本生成总结。
4. Markdown / TXT 文档基于原文生成总结。
5. 长文本分段摘要后合并。
6. 输出 {stem}.summary.md。
7. 总结文件进入全文索引。
```