# QBase v1.3 UI/UX 重塑实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 重塑 QBase 的端到端用户体验，包括三种布局模式、上下文感知 AI 菜单、AI 工作区四标签、命令面板等功能。

**Architecture:** 基于现有的 Vue 3 + Element Plus 架构，渐进式改造。使用 CSS Grid 实现布局模式切换，扩展 Pinia store 管理新状态，保持向后兼容。

**Tech Stack:** Vue 3, Element Plus, Pinia, CSS Grid, CSS Variables

---

## 前置检查

### 检查项目结构

**Files:**
- Read: `app/package.json`
- Read: `app/src/stores/ui.js`
- Read: `app/src/components/Layout/MainLayout.vue`

**Step 1: 确认当前依赖**

检查 `app/package.json` 确认以下依赖存在：
- `vue: ^3.5.x`
- `element-plus: ^2.13.x`
- `pinia: ^3.0.x`
- `pinia-plugin-persistedstate: ^4.7.x`

**Step 2: 确认 ui store 结构**

检查 `app/src/stores/ui.js` 当前状态字段。

**Step 3: 确认主布局结构**

检查 `app/src/components/Layout/MainLayout.vue` 当前实现。

---

## 阶段 1: 设计系统基础

### Task 1.1: 创建 CSS 变量文件

**Files:**
- Create: `app/src/styles/variables.css`

**Step 1: 创建 variables.css**

```css
/* Design Tokens - QBase v1.3 */
:root {
  /* Primary Colors */
  --primary-50: #f0f9ff;
  --primary-100: #e0f2fe;
  --primary-200: #bae6fd;
  --primary-300: #7dd3fc;
  --primary-400: #38bdf8;
  --primary-500: #0ea5e9;
  --primary-600: #0284c7;
  --primary-700: #0369a1;
  --primary-800: #075985;
  --primary-900: #0c4a6e;

  /* Accent Colors */
  --accent-warm: #f97316;
  --accent-success: #22c55e;
  --accent-warning: #eab308;
  --accent-danger: #ef4444;

  /* Neutral Colors (Light Theme) */
  --bg-primary: #ffffff;
  --bg-secondary: #f8fafc;
  --bg-tertiary: #f1f5f9;
  --text-primary: #0f172a;
  --text-secondary: #475569;
  --text-muted: #94a3b8;
  --border-subtle: #e2e8f0;
  --border-medium: #cbd5e1;

  /* Spacing */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;

  /* Border Radius */
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 12px;
  --radius-xl: 16px;
  --radius-full: 9999px;

  /* Shadows */
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.05);
  --shadow-md: 0 4px 6px -1px rgba(0,0,0,0.08);
  --shadow-lg: 0 10px 15px -3px rgba(0,0,0,0.1);
  --shadow-xl: 0 20px 25px -5px rgba(0,0,0,0.1);

  /* Transitions */
  --ease-out: cubic-bezier(0.21, 0.67, 0.24, 1.02);
  --ease-in-out: cubic-bezier(0.65, 0, 0.35, 1);
  --duration-fast: 150ms;
  --duration-base: 200ms;
  --duration-slow: 300ms;

  /* Typography */
  --font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
}
```

**Step 2: 提交**

```bash
cd app
git add src/styles/variables.css
git commit -m "feat: add design system css variables"
```

---

### Task 1.2: 创建主题文件

**Files:**
- Create: `app/src/styles/themes.css`

**Step 1: 创建 themes.css**

```css
/* Light Theme (Default) */
:root {
  --bg-primary: #ffffff;
  --bg-secondary: #f8fafc;
  --bg-tertiary: #f1f5f9;
  --text-primary: #0f172a;
  --text-secondary: #475569;
  --text-muted: #94a3b8;
  --border-subtle: #e2e8f0;
  --border-medium: #cbd5e1;
}

/* Dark Theme */
[data-theme="dark"] {
  --bg-primary: #0f172a;
  --bg-secondary: #1e293b;
  --bg-tertiary: #334155;
  --text-primary: #f8fafc;
  --text-secondary: #cbd5e1;
  --text-muted: #64748b;
  --border-subtle: #334155;
  --border-medium: #475569;
}

/* Element Plus Overrides */
:root {
  --el-color-primary: var(--primary-500);
  --el-color-primary-light-9: var(--primary-100);
  --el-bg-color-page: var(--bg-secondary);
  --el-bg-color: var(--bg-primary);
  --el-text-color-primary: var(--text-primary);
  --el-text-color-regular: var(--text-secondary);
  --el-text-color-secondary: var(--text-muted);
  --el-border-color: var(--border-subtle);
  --el-border-color-lighter: var(--border-subtle);
  --el-fill-color: var(--bg-tertiary);
}
```

**Step 2: 提交**

```bash
cd app
git add src/styles/themes.css
git commit -m "feat: add light/dark theme system"
```

---

### Task 1.3: 更新 main.js 引入样式

**Files:**
- Modify: `app/src/main.js`

**Step 1: 读取当前 main.js**

**Step 2: 添加样式导入**

在现有导入后添加：

```javascript
import './styles/variables.css'
import './styles/themes.css'
```

确保在 Element Plus 样式之后导入。

**Step 3: 提交**

```bash
cd app
git add src/main.js
git commit -m "feat: import design system styles"
```

---

## 阶段 2: 状态管理扩展

### Task 2.1: 扩展 ui store

**Files:**
- Modify: `app/src/stores/ui.js`

**Step 1: 读取当前 ui.js**

**Step 2: 添加新状态和 actions**

在 state 中添加：

```javascript
layoutMode: 'split', // 'split' | 'focus' | 'ai'
theme: 'light',     // 'light' | 'dark'
```

添加 actions：

```javascript
setLayoutMode(mode) {
  this.layoutMode = mode
},
setTheme(theme) {
  this.theme = theme
  document.documentElement.setAttribute('data-theme', theme)
},
```

在 persist.paths 中添加：

```javascript
'layoutMode',
'theme',
```

**Step 3: 提交**

```bash
cd app
git add src/stores/ui.js
git commit -m "feat: add layoutMode and theme to ui store"
```

---

## 阶段 3: 主布局升级

### Task 3.1: 重设计 MainLayout 顶部导航

**Files:**
- Modify: `app/src/components/Layout/MainLayout.vue`

**Step 1: 读取当前 MainLayout.vue**

**Step 2: 添加 Logo 组件**

在 template 的 header 中替换 logo 部分：

```vue
<div class="logo">
  <div class="logo-mark">QB</div>
  <span>QBase</span>
</div>
```

**Step 3: 添加布局模式切换按钮**

在 header 的 actions 区域添加：

```vue
<div class="mode-selector">
  <button 
    v-for="mode in layoutModes" 
    :key="mode"
    class="mode-btn"
    :class="{ active: uiStore.layoutMode === mode }"
    @click="uiStore.setLayoutMode(mode)"
    :title="modeLabels[mode]"
  >
    {{ modeIcons[mode] }}
  </button>
</div>
```

**Step 4: 添加 script 中的数据**

```javascript
const layoutModes = ['split', 'focus', 'ai']
const modeLabels = { split: '分栏模式', focus: '专注模式', ai: 'AI 模式' }
const modeIcons = { split: '🔄', focus: '📖', ai: '🤖' }
```

**Step 5: 添加 CSS 样式**

```css
.logo {
  display: flex;
  align-items: center;
  gap: 8px;
  font-weight: 700;
  font-size: 16px;
  color: var(--text-primary);
}

.logo-mark {
  width: 32px;
  height: 32px;
  background: linear-gradient(135deg, var(--primary-500), var(--primary-700));
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: 700;
  font-size: 14px;
}

.mode-selector {
  display: inline-flex;
  background: var(--bg-tertiary);
  border-radius: var(--radius-md);
  padding: 3px;
  gap: 2px;
}

.mode-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 6px 10px;
  border: none;
  background: transparent;
  border-radius: var(--radius-sm);
  cursor: pointer;
  font-size: 14px;
  transition: all var(--duration-base) var(--ease-out);
}

.mode-btn:hover {
  background: var(--bg-secondary);
}

.mode-btn.active {
  background: var(--bg-primary);
  box-shadow: var(--shadow-sm);
  color: var(--primary-600);
}
```

**Step 6: 提交**

```bash
cd app
git add src/components/Layout/MainLayout.vue
git commit -m "feat: redesign header with logo and mode selector"
```

---

### Task 3.2: 实现 CSS Grid 布局模式

**Files:**
- Modify: `app/src/components/Layout/MainLayout.vue`

**Step 1: 修改内容区域为 CSS Grid**

将原来的 flex 布局改为 grid：

```vue
<div class="main-area" :data-mode="uiStore.layoutMode">
  <Sidebar />
  <ContentPane />
  <AgentPanel />
</div>
```

**Step 2: 添加 CSS Grid 样式**

```css
.main-area {
  flex: 1;
  display: grid;
  grid-template-columns: 260px 1fr 42%;
  grid-template-rows: 1fr;
  height: calc(100vh - 48px);
  transition: all var(--duration-slow) var(--ease-in-out);
  overflow: hidden;
}

.main-area[data-mode="focus"] {
  grid-template-columns: 0 1fr 0;
}

.main-area[data-mode="ai"] {
  grid-template-columns: 0 30% 1fr;
}

.sidebar, .agent {
  overflow: hidden;
  transition: all var(--duration-slow) var(--ease-in-out);
  opacity: 1;
}

.main-area[data-mode="focus"] > .sidebar,
.main-area[data-mode="focus"] > .agent,
.main-area[data-mode="ai"] > .sidebar {
  opacity: 0;
  width: 0 !important;
  pointer-events: none;
}
```

**Step 3: 提交**

```bash
cd app
git add src/components/Layout/MainLayout.vue
git commit -m "feat: add css grid layout with three modes"
```

---

## 阶段 4: 上下文 AI 菜单

### Task 4.1: 创建 SelectionToolbar 组件

**Files:**
- Create: `app/src/components/shared/SelectionToolbar.vue`

**Step 1: 创建组件文件**

```vue
<template>
  <div 
    class="selection-toolbar"
    :class="{ show: visible }"
    :style="positionStyle"
  >
    <button class="toolbar-btn primary" @click="handleChat">
      💬 提问
    </button>
    <button class="toolbar-btn" @click="handleFlashcard">
      🃏 闪卡
    </button>
    <button class="toolbar-btn" @click="handleSummary">
      📑 摘要
    </button>
  </div>
</template>

<script setup>
import { computed } from 'vue'

const props = defineProps({
  visible: {
    type: Boolean,
    default: false
  },
  position: {
    type: Object,
    default: () => ({ left: 0, top: 0 })
  },
  selectedText: {
    type: String,
    default: ''
  }
})

const emit = defineEmits(['chat', 'flashcard', 'summary'])

const positionStyle = computed(() => ({
  left: `${props.position.left}px`,
  top: `${props.position.top}px`
}))

function handleChat() {
  emit('chat', props.selectedText)
}

function handleFlashcard() {
  emit('flashcard', props.selectedText)
}

function handleSummary() {
  emit('summary', props.selectedText)
}
</script>

<style scoped>
.selection-toolbar {
  position: absolute;
  background: var(--bg-primary);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-lg);
  padding: 6px;
  display: flex;
  gap: 4px;
  z-index: 30;
  transform: translateY(-10px);
  opacity: 0;
  pointer-events: none;
  transition: all var(--duration-base) var(--ease-out);
  border: 1px solid var(--border-medium);
}

.selection-toolbar.show {
  opacity: 1;
  pointer-events: auto;
  transform: translateY(0);
}

.toolbar-btn {
  padding: 8px 12px;
  border-radius: var(--radius-md);
  cursor: pointer;
  font-size: 13px;
  background: transparent;
  border: 1px solid transparent;
  transition: all var(--duration-fast) var(--ease-out);
  display: flex;
  align-items: center;
  gap: 6px;
  color: var(--text-secondary);
}

.toolbar-btn:hover {
  background: var(--bg-tertiary);
  border-color: var(--border-subtle);
  color: var(--text-primary);
}

.toolbar-btn.primary {
  background: var(--primary-50);
  color: var(--primary-700);
  border-color: var(--primary-200);
}

.toolbar-btn.primary:hover {
  background: var(--primary-100);
}
</style>
```

**Step 2: 提交**

```bash
cd app
git add src/components/shared/SelectionToolbar.vue
git commit -m "feat: add selection toolbar component"
```

---

### Task 4.2: 在 ContentPane 中集成选择监听

**Files:**
- Modify: `app/src/components/Layout/ContentPane.vue`

**Step 1: 读取当前 ContentPane.vue**

**Step 2: 导入 SelectionToolbar 组件**

```vue
import SelectionToolbar from '@/components/shared/SelectionToolbar.vue'
```

**Step 3: 添加选择监听逻辑**

```javascript
import { ref, onMounted, onUnmounted } from 'vue'

const selectedText = ref('')
const selectionVisible = ref(false)
const selectionPosition = ref({ left: 0, top: 0 })

function handleSelection() {
  const selection = window.getSelection()
  const text = selection.toString().trim()
  
  if (text.length > 4) {
    selectedText.value = text
    selectionVisible.value = true
    
    const range = selection.getRangeAt(0)
    const rect = range.getBoundingClientRect()
    const containerRect = document.querySelector('.doc-view')?.getBoundingClientRect()
    
    if (containerRect) {
      selectionPosition.value = {
        left: rect.left - containerRect.left + 20,
        top: rect.top - containerRect.top - 10
      }
    }
  } else {
    selectionVisible.value = false
  }
}

function handleToolbarChat(text) {
  console.log('Chat about:', text)
  selectionVisible.value = false
}

function handleToolbarFlashcard(text) {
  console.log('Generate flashcard:', text)
  selectionVisible.value = false
}

function handleToolbarSummary(text) {
  console.log('Generate summary:', text)
  selectionVisible.value = false
}

onMounted(() => {
  document.addEventListener('mouseup', handleSelection)
})

onUnmounted(() => {
  document.removeEventListener('mouseup', handleSelection)
})
```

**Step 4: 在模板中添加组件**

```vue
<SelectionToolbar
  :visible="selectionVisible"
  :position="selectionPosition"
  :selected-text="selectedText"
  @chat="handleToolbarChat"
  @flashcard="handleToolbarFlashcard"
  @summary="handleToolbarSummary"
/>
```

**Step 5: 提交**

```bash
cd app
git add src/components/Layout/ContentPane.vue
git commit -m "feat: integrate selection toolbar in content pane"
```

---

## 阶段 5: AI 面板四标签

### Task 5.1: 重设计 AgentPanel 标签导航

**Files:**
- Modify: `app/src/components/Layout/AgentPanel.vue`

**Step 1: 读取当前 AgentPanel.vue**

**Step 2: 添加标签导航 UI**

在模板顶部添加：

```vue
<div class="agent-tabs">
  <div 
    v-for="tab in agentTabs" 
    :key="tab.id"
    class="agent-tab"
    :class="{ active: activeTab === tab.id }"
    @click="activeTab = tab.id"
  >
    <span>{{ tab.icon }}</span>
    <span>{{ tab.name }}</span>
  </div>
</div>
```

**Step 3: 添加标签数据**

```javascript
import { ref } from 'vue'

const agentTabs = [
  { id: 'chat', name: '对话', icon: '💬' },
  { id: 'flashcard', name: '闪卡', icon: '🃏' },
  { id: 'mindmap', name: '导图', icon: '🧠' },
  { id: 'summary', name: '摘要', icon: '📊' }
]
const activeTab = ref('chat')
```

**Step 4: 添加条件渲染**

用 `v-if="activeTab === 'chat'"` 等包裹各模块内容。

**Step 5: 添加标签样式**

```css
.agent-tabs {
  display: flex;
  padding: 10px 12px;
  border-bottom: 1px solid var(--border-subtle);
  gap: 6px;
  background: var(--bg-secondary);
}

.agent-tab {
  flex: 1;
  padding: 8px 10px;
  border-radius: var(--radius-md);
  cursor: pointer;
  font-size: 12px;
  font-weight: 500;
  background: transparent;
  transition: all var(--duration-base) var(--ease-out);
  border: 1px solid transparent;
  text-align: center;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  color: var(--text-secondary);
}

.agent-tab:hover {
  background: var(--bg-tertiary);
}

.agent-tab.active {
  background: var(--bg-primary);
  color: var(--primary-600);
  border-color: var(--primary-200);
  box-shadow: var(--shadow-sm);
}
```

**Step 6: 提交**

```bash
cd app
git add src/components/Layout/AgentPanel.vue
git commit -m "feat: add four-tab navigation to agent panel"
```

---

## 阶段 6: 命令面板

### Task 6.1: 创建 CommandPalette 组件

**Files:**
- Create: `app/src/components/shared/CommandPalette.vue`

**Step 1: 创建组件文件**

```vue
<template>
  <div 
    class="command-overlay"
    :class="{ show: visible }"
    @click.self="close"
  >
    <div class="command-box">
      <div class="command-input-wrapper">
        <svg class="search-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="11" cy="11" r="8"></circle>
          <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
        </svg>
        <input 
          ref="inputRef"
          v-model="query"
          class="command-input"
          placeholder="⌘K 全局命令或搜索..."
          @keyup.esc="close"
          @keyup.enter="execute"
        >
      </div>
      <div class="command-filters">
        <span class="command-chip">📄 全文</span>
        <span class="command-chip">🤖 AI 生成</span>
        <span class="command-chip">🔄 重建索引</span>
        <span class="command-chip">🎨 切换深色</span>
      </div>
      <div class="command-footer">
        <span class="shortcut">
          <span class="kbd">↵</span> 确认执行
        </span>
        <span class="shortcut">
          <span class="kbd">ESC</span> 关闭
        </span>
      </div>
    </div>
  </div>
</template>

<script setup>
import { ref, watch, nextTick } from 'vue'

const props = defineProps({
  modelValue: {
    type: Boolean,
    default: false
  }
})

const emit = defineEmits(['update:modelValue', 'execute'])

const visible = ref(props.modelValue)
const query = ref('')
const inputRef = ref(null)

watch(() => props.modelValue, (val) => {
  visible.value = val
  if (val) {
    nextTick(() => inputRef.value?.focus())
  } else {
    query.value = ''
  }
})

watch(visible, (val) => {
  emit('update:modelValue', val)
})

function close() {
  visible.value = false
}

function execute() {
  emit('execute', query.value)
  close()
}

defineExpose({
  focus: () => inputRef.value?.focus()
})
</script>

<style scoped>
.command-overlay {
  position: fixed;
  inset: 0;
  background: rgba(15,23,42,0.45);
  backdrop-filter: blur(6px);
  z-index: 100;
  display: flex;
  justify-content: center;
  padding-top: 16vh;
  opacity: 0;
  pointer-events: none;
  transition: all var(--duration-base) var(--ease-out);
}

.command-overlay.show {
  opacity: 1;
  pointer-events: auto;
}

.command-box {
  width: 92%;
  max-width: 640px;
  background: var(--bg-primary);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-xl);
  overflow: hidden;
  transform: scale(0.96) translateY(10px);
  transition: all 0.25s cubic-bezier(0.34,1.56,0.64,1);
}

.command-overlay.show .command-box {
  transform: scale(1) translateY(0);
}

.command-input-wrapper {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 16px 20px;
  border-bottom: 1px solid var(--border-subtle);
}

.search-icon {
  width: 20px;
  height: 20px;
  color: var(--text-muted);
  flex-shrink: 0;
}

.command-input {
  width: 100%;
  padding: 0;
  border: none;
  outline: none;
  font-size: 16px;
  background: transparent;
  font-family: var(--font-body);
  color: var(--text-primary);
}

.command-input::placeholder {
  color: var(--text-muted);
}

.command-filters {
  padding: 12px 16px;
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.command-chip {
  padding: 5px 10px;
  background: var(--bg-secondary);
  border-radius: var(--radius-md);
  font-size: 12px;
  cursor: pointer;
  border: 1px solid var(--border-subtle);
  transition: all var(--duration-fast) var(--ease-out);
  color: var(--text-secondary);
  display: flex;
  align-items: center;
  gap: 6px;
}

.command-chip:hover {
  border-color: var(--primary-300);
  color: var(--primary-600);
  background: var(--primary-50);
}

.command-footer {
  padding: 12px 16px;
  background: var(--bg-secondary);
  font-size: 11px;
  color: var(--text-muted);
  display: flex;
  justify-content: space-between;
  border-top: 1px solid var(--border-subtle);
}

.shortcut {
  display: flex;
  align-items: center;
  gap: 8px;
}

.kbd {
  padding: 3px 6px;
  background: var(--bg-primary);
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-sm);
  font-family: var(--font-mono);
}
</style>
```

**Step 2: 提交**

```bash
cd app
git add src/components/shared/CommandPalette.vue
git commit -m "feat: add command palette component"
```

---

### Task 6.2: 在 MainLayout 中集成命令面板

**Files:**
- Modify: `app/src/components/Layout/MainLayout.vue`

**Step 1: 导入 CommandPalette**

```vue
import CommandPalette from '@/components/shared/CommandPalette.vue'
```

**Step 2: 添加状态和快捷键**

```javascript
import { ref, onMounted, onUnmounted } from 'vue'

const showCommandPalette = ref(false)

function handleKeyDown(e) {
  if ((e.metaKey || e.ctrlKey) && e.key === 'k') {
    e.preventDefault()
    showCommandPalette.value = true
  }
  if (e.key === 'Escape') {
    showCommandPalette.value = false
  }
}

function handleCommandExecute(query) {
  console.log('Execute command:', query)
}

onMounted(() => {
  document.addEventListener('keydown', handleKeyDown)
})

onUnmounted(() => {
  document.removeEventListener('keydown', handleKeyDown)
})
```

**Step 3: 在模板中添加**

```vue
<CommandPalette 
  v-model="showCommandPalette"
  @execute="handleCommandExecute"
/>
```

**Step 4: 提交**

```bash
cd app
git add src/components/Layout/MainLayout.vue
git commit -m "feat: integrate command palette with keyboard shortcut"
```

---

## 阶段 7: 工作区选择页面

### Task 7.1: 重设计 WorkspaceSelector

**Files:**
- Modify: `app/src/views/WorkspaceSelector.vue`

**Step 1: 读取当前 WorkspaceSelector.vue**

**Step 2: 更新模板为渐变背景设计**

```vue
<template>
  <div class="ws-page">
    <div class="ws-header">
      <h1>QBase</h1>
      <p>本地知识库 · AI 深度协作</p>
    </div>
    <div class="ws-grid">
      <div 
        v-for="ws in workspaces" 
        :key="ws.name"
        class="ws-card"
        @click="selectWorkspace(ws)"
      >
        <h3>
          <span>{{ ws.icon }}</span>
          {{ ws.name }}
        </h3>
        <div class="ws-path">{{ ws.path }}</div>
        <div class="ws-meta">
          <span>📄 {{ ws.files }} 文件</span>
          <span>🕐 {{ ws.recency }}</span>
        </div>
      </div>
      <div class="ws-card create" @click="createWorkspace">
        <div class="ws-icon">➕</div>
        <h3>创建新工作区</h3>
      </div>
    </div>
    <div class="ws-actions">
      <button class="ws-btn" @click="showDemo">📖 快速入门</button>
      <button class="ws-btn primary" @click="openExisting">📂 打开已有</button>
    </div>
  </div>
</template>
```

**Step 3: 添加样式**

```css
.ws-page {
  position: fixed;
  inset: 0;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: white;
}

.ws-header {
  text-align: center;
  margin-bottom: 32px;
}

.ws-header h1 {
  margin: 0;
  font-size: 36px;
  font-weight: 700;
  letter-spacing: -0.5px;
}

.ws-header p {
  margin-top: 8px;
  opacity: 0.9;
  font-size: 16px;
}

.ws-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 16px;
  width: 90%;
  max-width: 900px;
}

.ws-card {
  background: rgba(255,255,255,0.95);
  color: var(--text-primary);
  padding: 24px;
  border-radius: var(--radius-xl);
  cursor: pointer;
  transition: all var(--duration-base) var(--ease-out);
  box-shadow: var(--shadow-lg);
}

.ws-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 40px rgba(0,0,0,0.2);
}

.ws-card h3 {
  margin: 0 0 6px;
  font-size: 16px;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
}

.ws-card .ws-path {
  font-size: 13px;
  color: var(--text-muted);
  font-family: var(--font-mono);
}

.ws-card .ws-meta {
  display: flex;
  gap: 16px;
  font-size: 13px;
  color: var(--text-secondary);
  margin-top: 12px;
}

.ws-card.create {
  background: rgba(255,255,255,0.75);
  border: 2px dashed rgba(255,255,255,0.5);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.ws-card.create .ws-icon {
  font-size: 32px;
  margin-bottom: 8px;
}

.ws-card.create h3 {
  color: var(--text-secondary);
  justify-content: center;
}

.ws-actions {
  display: flex;
  gap: 16px;
  margin-top: 32px;
}

.ws-btn {
  padding: 12px 24px;
  border-radius: var(--radius-lg);
  background: rgba(255,255,255,0.2);
  border: 1px solid rgba(255,255,255,0.4);
  color: white;
  cursor: pointer;
  font-weight: 500;
  font-size: 14px;
  transition: all var(--duration-base) var(--ease-out);
  display: flex;
  align-items: center;
  gap: 8px;
}

.ws-btn:hover {
  background: rgba(255,255,255,0.35);
  transform: translateY(-1px);
}

.ws-btn.primary {
  background: white;
  color: #764ba2;
  border-color: transparent;
}

.ws-btn.primary:hover {
  background: rgba(255,255,255,0.95);
}
```

**Step 4: 提交**

```bash
cd app
git add src/views/WorkspaceSelector.vue
git commit -m "feat: redesign workspace selector with gradient background"
```

---

## 阶段 8: 测试和 Polish

### Task 8.1: 端到端测试

**Files:**
- Test: 手动测试所有功能

**Step 1: 启动开发服务器**

```bash
cd app
npm run dev
```

**Step 2: 测试清单**

- [ ] 三种布局模式正常切换
- [ ] 选中文本显示 AI 快捷菜单
- [ ] AI 面板四标签正常工作
- [ ] ⌘K / Ctrl+K 唤起命令面板
- [ ] 工作区选择页新设计
- [ ] 状态正确持久化（刷新页面）
- [ ] 无控制台错误

**Step 3: 跨平台测试**

在 Windows 和 macOS 上测试。

---

## 验收标准

### 功能验收
- [ ] 三种布局模式（split/focus/ai）正常切换
- [ ] 选中文本显示 AI 快捷菜单（💬 提问、🃏 闪卡、📑 摘要）
- [ ] AI 面板四标签（对话/闪卡/导图/摘要）正常工作
- [ ] ⌘K / Ctrl+K 唤起命令面板
- [ ] 工作区选择页紫色渐变背景
- [ ] 顶部导航有 Logo、布局模式切换按钮

### 视觉验收
- [ ] 配色符合设计系统 CSS 变量
- [ ] 动画流畅无卡顿
- [ ] 各组件视觉一致
- [ ] Element Plus 组件风格统一

### 质量验收
- [ ] 无控制台错误
- [ ] 性能无明显下降
- [ ] layoutMode 和 theme 状态正确持久化
- [ ] 代码提交记录清晰

---

Plan complete and saved to `docs/plans/2026-04-06-qbase-v1.3-ui-ux-redesign.md`. Two execution options:

**1. Subagent-Driven (this session)** - I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Parallel Session (separate)** - Open new session with executing-plans, batch execution with checkpoints

Which approach?
