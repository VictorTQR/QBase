# QBase v0.1 实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标:** 实现 QBase v0.1 - 本地知识库核心浏览功能

**架构:** 三栏布局 + Pinia状态管理 + Electron文件系统API

**技术栈:** Vue 3 + Electron + Pinia + Element Plus + Element-Plus-X

---

## 准备工作

### Task 0: 安装依赖

**文件:**
- Modify: `app/package.json`
- Run: `npm install`

**步骤:**
1. 查看当前 package.json
2. 添加 element-plus 和 element-plus-x 依赖
3. 运行 npm install
4. 验证安装成功

---

## v0.1 核心功能实施

### Task 1: 配置 Element Plus 和 Element-Plus-X

**文件:**
- Modify: `app/src/main.js`
- Create: `app/src/styles/element-plus.css`

**步骤:**
1. 在 main.js 中引入 Element Plus 和样式
2. 引入 Element-Plus-X 样式
3. app.use(ElementPlus)
4. 测试配置是否生效

---

### Task 2: 创建 Workspace Store

**文件:**
- Create: `app/src/stores/workspace.js`

**步骤:**
1. 创建 Pinia store
2. 定义 state: folders, activeFileId, fileTree
3. 定义 actions: addFolder, removeFolder, refreshFileTree, selectFile
4. 配置持久化
5. 编写测试

---

### Task 3: 扩展 Electron Preload API

**文件:**
- Modify: `app/electron/preload.js`
- Modify: `app/electron/main.js`

**步骤:**
1. 添加 selectFolder API
2. 添加 readFile API
3. 添加 readDir API
4. 添加 getFileStats API
5. 添加 searchFiles API（基础实现）
6. 测试 API 可用性

---

### Task 4: 创建主布局组件

**文件:**
- Create: `app/src/components/Layout/MainLayout.vue`

**步骤:**
1. 三栏布局容器
2. 顶部导航栏
3. 左侧、中间、右侧栏占位
4. 拖拽调整宽度功能
5. 响应式布局

---

### Task 5: 创建左侧文件夹树组件

**文件:**
- Create: `app/src/components/Layout/Sidebar.vue`

**步骤:**
1. 使用 el-tree 组件
2. 从 workspace store 获取数据
3. 实现文件树递归展示
4. 点击选中文件
5. 展开/折叠功能
6. 样式调整

---

### Task 6: 创建 MarkdownViewer 组件

**文件:**
- Create: `app/src/components/MarkdownViewer.vue`

**步骤:**
1. 使用 XMarkdown 组件
2. Props: content
3. 配置 enableLatex, enableBreaks
4. 测试渲染效果
5. 测试代码高亮、数学公式

---

### Task 7: 创建中间内容区组件

**文件:**
- Create: `app/src/components/Layout/ContentPane.vue`

**步骤:**
1. 顶部显示当前文件路径
2. 集成 MarkdownViewer
3. 从 document store 获取内容
4. 读取文件内容逻辑
5. 错误处理（文件读取失败）

---

### Task 8: 创建 Document Store

**文件:**
- Create: `app/src/stores/document.js`

**步骤:**
1. state: currentFile, content, isLoading, error
2. actions: loadFile, clearContent
3. 通过 Electron API 读取文件
4. 错误处理
5. 编写测试

---

### Task 9: 创建右侧 Agent 面板（占位）

**文件:**
- Create: `app/src/components/Layout/AgentPanel.vue`

**步骤:**
1. 占位组件
2. 显示 "v0.2 敬请期待"
3. 可折叠/展开按钮
4. 预留布局空间

---

### Task 10: 创建首页视图

**文件:**
- Modify: `app/src/views/Home.vue`
- Create: `app/src/views/Home.vue`（如果不存在）

**步骤:**
1. 集成 MainLayout
2. 组合 Sidebar, ContentPane, AgentPanel
3. 状态管理连接
4. 测试整体布局

---

### Task 11: 实现基础搜索功能

**文件:**
- Create: `app/src/stores/search.js`
- Create: `app/src/components/SearchBox.vue`
- Modify: `app/src/components/Layout/MainLayout.vue`

**步骤:**
1. Search store: query, results, isSearching
2. 搜索框组件
3. Electron API 的 searchFiles 实现
4. 搜索结果展示
5. 点击结果跳转到文件

---

### Task 12: 路由配置

**文件:**
- Modify: `app/src/router/index.js`

**步骤:**
1. 配置首页路由
2. 测试路由跳转

---

### Task 13: 集成测试和整体测试

**文件:**
- Test: `app/src/__tests__/` 下的测试文件

**步骤:**
1. 运行所有单元测试
2. 手动测试完整流程
3. 添加文件夹
4. 浏览文件树
5. 查看 Markdown 渲染
6. 测试搜索功能
7. 验证错误处理

---

### Task 14: 代码格式化和 lint

**文件:**
- All files

**步骤:**
1. 运行 `npm run format`
2. 运行 `npm run lint`
3. 修复所有 lint 错误
4. 确保代码风格一致

---

## 里程碑检查

### v0.1 完成标准
- [ ] 可以添加/移除本地文件夹
- [ ] 文件夹树正常展示
- [ ] Markdown 文件正确渲染
- [ ] 基础搜索功能可用
- [ ] 所有测试通过
- [ ] lint 和 format 通过

---

## 后续阶段（v0.2+ 预留）

- Agent 对话面板
- LLM 配置管理
- 基于文档的问答
- 标签系统
- 搜索增强
