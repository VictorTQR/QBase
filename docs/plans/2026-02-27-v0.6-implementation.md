# v0.6 智能体能力增强 - 实施计划

&gt; **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 实现 v0.6 智能体能力增强，包括 MinerU PDF 解析、SiliconFlow 向量化搜索，以及预埋 DocumentProcessor 和 VectorStore 统一接口层。

**Architecture:** 采用 B+ 架构，先定义统一接口，再用云端 API 实现，预留后期本地模型替换空间。

**Tech Stack:** Electron IPC、MinerU API、SiliconFlow API、内存向量索引

---

## 目录

1. [准备工作](#1-准备工作)
2. [架构层：统一接口定义](#2-架构层统一接口定义)
3. [Electron 主进程：MinerU 集成](#3-electron-主进程mineru-集成)
4. [向量存储：MemoryVectorStore 实现](#4-向量存储memoryvectorstore-实现)
5. [Store 层：agent.js 扩展](#5-store-层agentjs-扩展)
6. [UI 层：配置对话框扩展](#6-ui-层配置对话框扩展)
7. [集成与测试](#7-集成与测试)

---

## 1. 准备工作

### Task 1.1: 探索现有代码结构

**Files:**
- Read: `app/src/stores/agent.js`
- Read: `app/src/utils/api.js`
- Read: `app/electron/main.js`
- Read: `app/electron/preload.js`

**Step 1: 读取关键文件**

使用 Read 工具读取上述四个文件，了解现有代码结构。

---

## 2. 架构层：统一接口定义

### Task 2.1: 创建 processors 目录和接口定义

**Files:**
- Create: `app/src/processors/index.js`
- Create: `app/src/processors/DocumentProcessor.js`
- Create: `app/src/processors/MinerUProcessor.js`
- Create: `app/src/vector/index.js`
- Create: `app/src/vector/VectorStore.js`
- Create: `app/src/vector/MemoryVectorStore.js`

**Step 1: 创建 DocumentProcessor 接口**

```javascript
// app/src/processors/DocumentProcessor.js

export class DocumentProcessor {
  async extractText(file) {
    throw new Error('extractText not implemented')
  }

  async extractStructured(file) {
    throw new Error('extractStructured not implemented')
  }
}
```

**Step 2: 创建 VectorStore 接口**

```javascript
// app/src/vector/VectorStore.js

export class VectorStore {
  async addDocument(doc) {
    throw new Error('addDocument not implemented')
  }

  async search(query, k = 5) {
    throw new Error('search not implemented')
  }

  async clear() {
    throw new Error('clear not implemented')
  }
}
```

**Step 3: 创建 index.js 导出**

```javascript
// app/src/processors/index.js
export { DocumentProcessor } from './DocumentProcessor.js'
export { MinerUProcessor } from './MinerUProcessor.js'

// app/src/vector/index.js
export { VectorStore } from './VectorStore.js'
export { MemoryVectorStore } from './MemoryVectorStore.js'
```

---

## 3. Electron 主进程：MinerU 集成

### Task 3.1: 扩展 preload.js API

**Files:**
- Modify: `app/electron/preload.js`

**Step 1: 添加新的 electronAPI 方法**

在 `contextBridge.exposeInMainWorld` 中添加：

```javascript
mineru: {
  createUploadUrls: (files) => ipcRenderer.invoke('mineru:create-upload-urls', files),
  uploadFile: (url, fileData) => ipcRenderer.invoke('mineru:upload-file', url, fileData),
  submitTask: (batchId) => ipcRenderer.invoke('mineru:submit-task', batchId),
  pollTaskStatus: (taskId) => ipcRenderer.invoke('mineru:poll-task-status', taskId),
  downloadResult: (url) => ipcRenderer.invoke('mineru:download-result', url),
  extractPdf: (filePath, apiKey) => ipcRenderer.invoke('mineru:extract-pdf', filePath, apiKey)
},
siliconflow: {
  createEmbedding: (text, config) => ipcRenderer.invoke('siliconflow:create-embedding', text, config)
}
```

### Task 3.2: 扩展 main.js IPC 处理器

**Files:**
- Modify: `app/electron/main.js`

**Step 1: 添加 MinerU IPC 处理器**

在 `main.js` 中添加：

```javascript
const https = require('https')
const http = require('http')
const fs = require('fs')
const path = require('path')
const { URL } = require('url')

// Helper: HTTP request
function makeRequest(options, data = null) {
  return new Promise((resolve, reject) => {
    const lib = options.protocol === 'https:' ? https : http
    const req = lib.request(options, (res) => {
      let body = ''
      res.on('data', (chunk) => body += chunk)
      res.on('end', () => {
        try {
          const result = JSON.parse(body)
          resolve({ statusCode: res.statusCode, data: result })
        } catch (e) {
          resolve({ statusCode: res.statusCode, data: body })
        }
      })
    })
    req.on('error', reject)
    if (data) req.write(JSON.stringify(data))
    req.end()
  })
}

// Helper: PUT upload
function uploadFile(url, fileData) {
  return new Promise((resolve, reject) => {
    const parsedUrl = new URL(url)
    const options = {
      hostname: parsedUrl.hostname,
      port: parsedUrl.port || (parsedUrl.protocol === 'https:' ? 443 : 80),
      path: parsedUrl.pathname + parsedUrl.search,
      method: 'PUT'
    }
    const lib = parsedUrl.protocol === 'https:' ? https : http
    const req = lib.request(options, (res) => {
      let body = ''
      res.on('data', (chunk) => body += chunk)
      res.on('end', () => resolve({ statusCode: res.statusCode, data: body }))
    })
    req.on('error', reject)
    req.write(fileData)
    req.end()
  })
}

// Helper: Download file
function downloadFile(url) {
  return new Promise((resolve, reject) => {
    const lib = url.startsWith('https') ? https : http
    lib.get(url, (res) => {
      const chunks = []
      res.on('data', (chunk) => chunks.push(chunk))
      res.on('end', () => resolve(Buffer.concat(chunks)))
    }).on('error', reject)
  })
}

// MinerU IPC handlers
ipcMain.handle('mineru:create-upload-urls', async (event, files, apiKey) =&gt; {
  const url = 'https://mineru.net/api/v4/file-urls/batch'
  const options = {
    hostname: 'mineru.net',
    port: 443,
    path: '/api/v4/file-urls/batch',
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${apiKey}`
    }
  }
  const result = await makeRequest(options, { files, model_version: 'vlm' })
  return result.data
})

ipcMain.handle('mineru:upload-file', async (event, url, fileData) =&gt; {
  return await uploadFile(url, fileData)
})

ipcMain.handle('mineru:submit-task', async (event, batchId, apiKey) =&gt; {
  const options = {
    hostname: 'mineru.net',
    port: 443,
    path: `/api/v4/extract-results/batch/${batchId}`,
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${apiKey}`
    }
  }
  const result = await makeRequest(options)
  return result.data
})

ipcMain.handle('mineru:poll-task-status', async (event, taskId, apiKey) =&gt; {
  const options = {
    hostname: 'mineru.net',
    port: 443,
    path: `/api/v4/extract/task/${taskId}`,
    method: 'GET',
    headers: {
      'Authorization': `Bearer ${apiKey}`
    }
  }
  const result = await makeRequest(options)
  return result.data
})

ipcMain.handle('mineru:download-result', async (event, url) =&gt; {
  return await downloadFile(url)
})

// 一站式 PDF 解析（简化版）
ipcMain.handle('mineru:extract-pdf', async (event, filePath, apiKey) =&gt; {
  const fileName = path.basename(filePath)
  const fileData = fs.readFileSync(filePath)

  // Step 1: 申请上传链接
  const uploadResult = await makeRequest({
    hostname: 'mineru.net',
    port: 443,
    path: '/api/v4/file-urls/batch',
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${apiKey}`
    }
  }, {
    files: [{ name: fileName }],
    model_version: 'vlm'
  })

  if (uploadResult.data.code !== 0) {
    throw new Error(uploadResult.data.msg || 'Failed to get upload URL')
  }

  const batchId = uploadResult.data.data.batch_id
  const uploadUrl = uploadResult.data.data.file_urls[0]

  // Step 2: 上传文件
  await uploadFile(uploadUrl, fileData)

  // Step 3: 等待并轮询结果
  await new Promise(resolve =&gt; setTimeout(resolve, 5000)) // 初始等待

  let maxAttempts = 60
  let attempts = 0
  let taskResult = null

  while (attempts &lt; maxAttempts) {
    const pollResult = await makeRequest({
      hostname: 'mineru.net',
      port: 443,
      path: `/api/v4/extract-results/batch/${batchId}`,
      method: 'GET',
      headers: {
        'Authorization': `Bearer ${apiKey}`
      }
    })

    if (pollResult.data.code === 0 &amp;&amp; pollResult.data.data.extract_result) {
      const fileResult = pollResult.data.data.extract_result[0]
      if (fileResult.state === 'done') {
        taskResult = fileResult
        break
      } else if (fileResult.state === 'failed') {
        throw new Error(fileResult.err_msg || 'Parsing failed')
      }
    }

    attempts++
    await new Promise(resolve =&gt; setTimeout(resolve, 3000))
  }

  if (!taskResult) {
    throw new Error('Timeout waiting for parsing result')
  }

  // Step 4: 下载结果 ZIP
  const zipBuffer = await downloadFile(taskResult.full_zip_url)

  // Step 5: 简单解析 ZIP 并提取 markdown（需要引入 adm-zip 或类似库）
  // 这里简化处理，实际需要解压
  return {
    success: true,
    zipUrl: taskResult.full_zip_url,
    // 后续需要解压提取 markdown
  }
})

// SiliconFlow IPC handler
ipcMain.handle('siliconflow:create-embedding', async (event, text, config) =&gt; {
  const options = {
    hostname: 'api.siliconflow.cn',
    port: 443,
    path: '/v1/embeddings',
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${config.apiKey}`
    }
  }
  const result = await makeRequest(options, {
    model: config.model || 'BAAI/bge-large-zh-v1.5',
    input: text
  })
  return result.data
})
```

---

## 4. 向量存储：MemoryVectorStore 实现

### Task 4.1: 实现 MemoryVectorStore

**Files:**
- Create/Modify: `app/src/vector/MemoryVectorStore.js`

**Step 1: 实现内存向量存储**

```javascript
// app/src/vector/MemoryVectorStore.js
import { VectorStore } from './VectorStore.js'

// 简单余弦相似度计算
function cosineSimilarity(a, b) {
  if (!a || !b || a.length !== b.length) return 0
  let dotProduct = 0
  let normA = 0
  let normB = 0
  for (let i = 0; i &lt; a.length; i++) {
    dotProduct += a[i] * b[i]
    normA += a[i] * a[i]
    normB += b[i] * b[i]
  }
  return dotProduct / (Math.sqrt(normA) * Math.sqrt(normB))
}

export class MemoryVectorStore extends VectorStore {
  constructor() {
    super()
    this.documents = []
    this.embeddings = []
  }

  async addDocument(doc) {
    this.documents.push(doc)
    if (doc.embedding) {
      this.embeddings.push(doc.embedding)
    }
  }

  async search(queryEmbedding, k = 5) {
    const results = []
    for (let i = 0; i &lt; this.documents.length; i++) {
      const doc = this.documents[i]
      const embedding = this.embeddings[i]
      if (embedding) {
        const score = cosineSimilarity(queryEmbedding, embedding)
        results.push({
          id: doc.id,
          content: doc.content,
          score,
          metadata: doc.metadata
        })
      }
    }
    return results.sort((a, b) =&gt; b.score - a.score).slice(0, k)
  }

  async clear() {
    this.documents = []
    this.embeddings = []
  }
}
```

---

## 5. Store 层：agent.js 扩展

### Task 5.1: 扩展 agent.js store

**Files:**
- Modify: `app/src/stores/agent.js`

**Step 1: 添加配置扩展**

在 `llmConfig` 的默认值中添加：

```javascript
mineru: {
  apiKey: '',
  baseUrl: 'https://mineru.net'
},
siliconflow: {
  apiKey: '',
  baseUrl: 'https://api.siliconflow.cn',
  embeddingModel: 'BAAI/bge-large-zh-v1.5',
  asrModel: 'FunAudioLLM/SenseVoiceSmall'
}
```

**Step 2: 添加新方法**

在 `agent.js` 中添加：

```javascript
import { MinerUProcessor } from '@/processors/index.js'
import { MemoryVectorStore } from '@/vector/index.js'

let mineruProcessor = null
let vectorStore = null

// 在 store 内部初始化
function getMinerUProcessor() {
  if (!mineruProcessor) {
    mineruProcessor = new MinerUProcessor(llmConfig.mineru)
  }
  return mineruProcessor
}

function getVectorStore() {
  if (!vectorStore) {
    vectorStore = new MemoryVectorStore()
  }
  return vectorStore
}

// 新增方法
const extractPdfWithMinerU = async (filePath) =&gt; {
  const processor = getMinerUProcessor()
  return await processor.extractText({ path: filePath })
}

const createEmbedding = async (text) =&gt; {
  const config = llmConfig.siliconflow
  if (!config.apiKey) {
    throw new Error('SiliconFlow API key not configured')
  }
  return await window.electronAPI.siliconflow.createEmbedding(text, config)
}

const addToVectorStore = async (doc) =&gt; {
  const store = getVectorStore()
  return await store.addDocument(doc)
}

const searchVectorStore = async (queryEmbedding, k = 5) =&gt; {
  const store = getVectorStore()
  return await store.search(queryEmbedding, k)
}
```

---

## 6. UI 层：配置对话框扩展

### Task 6.1: 扩展 LlmConfigDialog.vue

**Files:**
- Modify: `app/src/components/LlmConfigDialog.vue`

**Step 1: 添加 MinerU 和 SiliconFlow 配置表单**

在现有表单中添加两个新的配置区域：

```vue
&lt;el-tabs v-model="activeConfigTab"&gt;
  &lt;!-- 现有 LLM 配置 tab --&gt;
  &lt;el-tab-pane label="LLM" name="llm"&gt;
    &lt;!-- 现有内容 --&gt;
  &lt;/el-tab-pane&gt;

  &lt;!-- 新增 MinerU 配置 --&gt;
  &lt;el-tab-pane label="MinerU (PDF)" name="mineru"&gt;
    &lt;el-form-item label="API Key"&gt;
      &lt;el-input
        v-model="localConfig.mineru.apiKey"
        type="password"
        show-password
        placeholder="请输入 MinerU API Key"
      /&gt;
    &lt;/el-form-item&gt;
    &lt;el-form-item label="Base URL"&gt;
      &lt;el-input v-model="localConfig.mineru.baseUrl" /&gt;
    &lt;/el-form-item&gt;
  &lt;/el-tab-pane&gt;

  &lt;!-- 新增 SiliconFlow 配置 --&gt;
  &lt;el-tab-pane label="SiliconFlow" name="siliconflow"&gt;
    &lt;el-form-item label="API Key"&gt;
      &lt;el-input
        v-model="localConfig.siliconflow.apiKey"
        type="password"
        show-password
        placeholder="请输入 SiliconFlow API Key"
      /&gt;
    &lt;/el-form-item&gt;
    &lt;el-form-item label="Embedding Model"&gt;
      &lt;el-select v-model="localConfig.siliconflow.embeddingModel"&gt;
        &lt;el-option label="BAAI/bge-large-zh-v1.5" value="BAAI/bge-large-zh-v1.5" /&gt;
        &lt;el-option label="BAAI/bge-m3" value="BAAI/bge-m3" /&gt;
      &lt;/el-select&gt;
    &lt;/el-form-item&gt;
    &lt;el-form-item label="ASR Model"&gt;
      &lt;el-select v-model="localConfig.siliconflow.asrModel"&gt;
        &lt;el-option label="FunAudioLLM/SenseVoiceSmall" value="FunAudioLLM/SenseVoiceSmall" /&gt;
        &lt;el-option label="TeleAI/TeleSpeechASR" value="TeleAI/TeleSpeechASR" /&gt;
      &lt;/el-select&gt;
    &lt;/el-form-item&gt;
  &lt;/el-tab-pane&gt;
&lt;/el-tabs&gt;
```

---

## 7. 集成与测试

### Task 7.1: 实现 MinerUProcessor

**Files:**
- Create/Modify: `app/src/processors/MinerUProcessor.js`

**Step 1: 实现 MinerUProcessor**

```javascript
// app/src/processors/MinerUProcessor.js
import { DocumentProcessor } from './DocumentProcessor.js'

export class MinerUProcessor extends DocumentProcessor {
  constructor(config) {
    super()
    this.config = config
  }

  async extractText(file) {
    if (!this.config.apiKey) {
      throw new Error('MinerU API key not configured')
    }
    const result = await window.electronAPI.mineru.extractPdf(
      file.path,
      this.config.apiKey
    )
    return result
  }

  async extractStructured(file) {
    return await this.extractText(file)
  }
}
```

### Task 7.2: 运行 lint 和 format

**Files:**
- Run: `npm run lint`
- Run: `npm run format`

**Step 1: 运行代码检查和格式化**

```bash
cd app
npm run lint
npm run format
```

---

## 实施总结

### 完成的文件清单

| 操作 | 文件路径 |
|------|---------|
| Create | `app/src/processors/DocumentProcessor.js` |
| Create | `app/src/processors/MinerUProcessor.js` |
| Create | `app/src/processors/index.js` |
| Create | `app/src/vector/VectorStore.js` |
| Create | `app/src/vector/MemoryVectorStore.js` |
| Create | `app/src/vector/index.js` |
| Modify | `app/electron/preload.js` |
| Modify | `app/electron/main.js` |
| Modify | `app/src/stores/agent.js` |
| Modify | `app/src/components/LlmConfigDialog.vue` |

### 测试步骤

1. 配置 MinerU API Key
2. 配置 SiliconFlow API Key
3. 选择一个 PDF 文件进行测试
4. 测试向量搜索功能

---

**Plan complete and saved to `docs/plans/2026-02-27-v0.6-implementation.md`. Two execution options:**

**1. Subagent-Driven (this session)** - I dispatch fresh subagent per task, review between tasks, fast iteration

**2. Parallel Session (separate)** - Open new session with executing-plans, batch execution with checkpoints

**Which approach?**
